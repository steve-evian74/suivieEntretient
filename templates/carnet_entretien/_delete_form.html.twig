<form method="post" action="{{ path('carnet_entretien_delete', {'id': carnet_entretien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carnet_entretien.id) }}">
    <button class="btn">Delete</button>
</form>