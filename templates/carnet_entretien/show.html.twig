{% extends 'base.html.twig' %}

{% block title %}CarnetEntretien{% endblock %}

{% block body %}

    <h1>CarnetEntretien</h1>

    {{dump(carnet_entretien)}}
    {% set id = app.user.id %}
    Id de la session : {{id}} <br/>
    Id de la propriètaire : {{carnet_entretien.ficheVoiture.user.id}}
    {%if id%}
            {%if id == carnet_entretien.ficheVoiture.user.id%}
                <table class="table">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ carnet_entretien.id }}</td>
                        </tr>
                        <tr>
                            <th>DateIntervention</th>
                            <td>{{ carnet_entretien.dateIntervention ? carnet_entretien.dateIntervention|date('Y-m-d') : '' }}</td>
                        </tr>
                        <tr>
                            <th>Kilometre</th>
                            <td>{{ carnet_entretien.kilometre }}</td>
                        </tr>
                        <tr>
                            <th>CommentaireIntervention</th>
                            <td>{{ carnet_entretien.commentaireIntervention }}</td>
                        </tr>
                    </tbody>
                </table>
                <a href="{{ path('carnet_entretien_index') }}">back to list</a>

                <a href="{{ path('carnet_entretien_edit', {'id': carnet_entretien.id}) }}">edit</a>

                {{ include('carnet_entretien/_delete_form.html.twig') }}
            {%endif%}
        {%endif%}

{% endblock %}